# Preconditions
- [ ] Snapshot references resolve to immutable content-addressed data
- [ ] Governance config version is signed and within policy
- [ ] Executor identity is authorized to run decisions
- [ ] Deterministic runtime profile is available (pinned threads, float64 strict)
